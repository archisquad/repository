{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "test": {
      "inputs": ["src/**", "test/**"],
      "outputs": ["coverage/**"]
    },
    "lint": {},
    "type-check": {},
    "pub-check": {},
    "dev": {
      "cache": false
    }
  }
}
